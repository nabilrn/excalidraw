# Google OAuth Desktop client ID used by FocusCanvas Google Drive appData sync.
VITE_GOOGLE_CLIENT_ID=

# Client secret from the same Google OAuth Desktop client.
# Desktop apps cannot keep this value confidential, so it is not a security boundary;
# FocusCanvas still keeps it out of the frontend JavaScript bundle and source control.
GOOGLE_CLIENT_SECRET=
